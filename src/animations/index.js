/**
 * Register shared GSAP defaults or plugins if needed.
 * Section-level animations live next to their components.
 */
export function registerGlobalMotion() {
  // Reserved for future shared animation setup.
}
